As a fable agent your task is to evaluate everything and plan everything to create a task document for a opus agent to follow and act as an orchastrator and make (opus, sonnet, haiku) models work on depending on intensity and requirement. the plan document should have all the info for the opus agent to follow and utilize. the document should be broken down into segments with reviewer segments in between after notable progress to ensure progress is on track. At the very end the fable agent to review the entire project again.

there should be another progress.md document that the opus needs to update  gradually so that it may be used as a tracker in case work is done in segments or work is halted in the middle and the 2 docs can be used to resume/restart without any other issues.

the task of the project is to create a python script which will get 2 data's as input (a center point (latitude, longitude) and a radius). the script needs to the lat/lon of all the the roads that fall under that circle and document that data in a csv format as provided in the @data/Road_Network - Road_Network.csv example data. the data should also contain info about the roads like the road widths and etc. feel free to add more columns to the data table if required. all the data should be extracted from a google maps api or something free avaiable and up to date. 

after extraction and data creation is complete. simply convert the csv file to an xlsx doc

before progressing feel free to ask as many questions as required for further clarification

the csv file that i have provided has all the road length, start point etc but it doesnt really specify the coordinate/shape of the roads. And this has data for all the roads in Western Australia. this data is from the roads that fall inside the 10km radius area with the center point being curtin school of design and built environment building (Centre point: -32.0018629, 115.8924599) but do not contain the exact start and end coordinates and the width of those roads which are required

just for this project committing and pushing without specific instructions are allowed.